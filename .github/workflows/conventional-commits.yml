name: Conventional Commits Lint
on:
  pull_request:
    branches: [ main ]

jobs:
  commit-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint commit messages
        uses: amitsingh-007/commitlint-action@v4
        with:
          config: conventional
